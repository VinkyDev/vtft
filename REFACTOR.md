目前,线上的数据运作方式是
  @apps/server 启动爬虫定时任务,通过 @packages/crawler 爬虫, 通过 @packages/db 将数据保存到数据库,然后读取.

  但是! 原有的数据来源目前无法正常到数据了,且原有的爬虫方式数据不全,不稳定.
  现在要大规模的重构整个应用!
  用新的 @packages/scraper 包通过合规的抓第三方 API 的方式获取数据, 用合适的结构保存到数据库然后前端使用.
  目前能爬到的数据都在这里了 @packages/scraper/src/meta-tft.ts
  这次重构范围很广,需要完整的重构服务爬虫机制, 数据库存储, 类型定义 @packages/types ,前端 @apps/react 数据处理,数据展示等等.
  请你设计一个周全的方案,完整这次大重构. (不需要兼容老逻辑,这是一个还没上线的新应用,要的是大刀阔斧的重构!)

  整个重构过程中，肯定会涉及到完全重写的文件和逻辑，在重构时可以根据需要进行整体逻辑的大优化和重构，不需要完全按照老逻辑写。
  再次提醒需要注意的代码规范：DRY，不要过度设计，只写有意义的注释，保持代码的可读性和可维护性。
